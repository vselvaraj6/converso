name: Deploy to homelab

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy
    runs-on: self-hosted
    timeout-minutes: 15

    env:
      # Pin the Compose project name so Docker always refers to the same
      # stack regardless of which directory the runner checks out into.
      COMPOSE_PROJECT_NAME: converso

    steps:
      - name: Checkout latest code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Write .env from GitHub Secrets
        run: |
          cat > .env <<'ENVEOF'
          SECRET_KEY=${{ secrets.SECRET_KEY }}
          TWILIO_ACCOUNT_SID=${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN=${{ secrets.TWILIO_AUTH_TOKEN }}
          TWILIO_PHONE_NUMBER=${{ secrets.TWILIO_PHONE_NUMBER }}
          OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL=gpt-4-turbo
          VAPI_API_KEY=${{ secrets.VAPI_API_KEY }}
          VAPI_BASE_URL=https://api.vapi.ai
          CALCOM_BASE_URL=${{ secrets.CALCOM_BASE_URL }}
          CALCOM_API_KEY=${{ secrets.CALCOM_API_KEY }}
          CALCOM_WEBHOOK_SECRET=${{ secrets.CALCOM_WEBHOOK_SECRET }}
          ENVEOF

      - name: Fix Docker networking
        run: |
          echo "→ Tearing down existing stack..."
          docker compose down --remove-orphans 2>/dev/null || true

          echo "→ Recreating missing Docker isolation chains..."
          sudo iptables -N DOCKER-ISOLATION-STAGE-1 || true
          sudo iptables -N DOCKER-ISOLATION-STAGE-2 || true
          sudo iptables -A FORWARD -j DOCKER-ISOLATION-STAGE-1 || true
          sudo iptables -A DOCKER-ISOLATION-STAGE-1 ! -i docker0 -o docker0 -j DOCKER-ISOLATION-STAGE-2 || true
          sudo iptables -A DOCKER-ISOLATION-STAGE-1 -j RETURN || true
          sudo iptables -A DOCKER-ISOLATION-STAGE-2 -j RETURN || true

          echo "→ Restarting Docker daemon to ensure clean state..."
          sudo systemctl restart docker
          sleep 5

      - name: Rebuild and restart containers
        run: |
          echo "→ Rebuilding containers..."
          docker compose build
          docker compose up -d --remove-orphans

          echo "→ Waiting for database..."
          sleep 10

          echo "→ Running database migrations..."
          docker compose exec -T app alembic upgrade head

          echo "→ Removing dangling images..."
          docker image prune -f

      - name: Health check
        run: |
          echo "→ Waiting for app to be ready on port 8080..."
          for i in $(seq 1 12); do
            if curl -sf http://localhost:8080/api/health; then
              echo "Health check passed."
              exit 0
            fi
            echo "  attempt $i/12 — retrying in 5s..."
            sleep 5
          done
          echo "Health check failed after 60s!" >&2
          docker compose logs --tail=50 app >&2
          exit 1

      - name: Notify success
        if: success()
        run: echo "✓ Converso deployed successfully at $(date)"

      - name: Notify failure
        if: failure()
        run: echo "✗ Deployment FAILED at $(date)" >&2
